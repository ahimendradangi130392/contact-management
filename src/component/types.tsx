export interface IViewContact {
    map(arg0: (_item: any) => import("react/jsx-runtime").JSX.Element): unknown;
    id: any,
    firstName: string,
    lastName: string
}
export interface IEditState {
    formVisibility: boolean
}